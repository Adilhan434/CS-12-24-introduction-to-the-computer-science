database and bigData. database is can be build on SQL or my SQL or e.tc . every stolbec contains some data and can be used by the command get.
bigdata is used by big companies, and they analysys data of their clients.
primery key is identy for every stolb that contains some data
